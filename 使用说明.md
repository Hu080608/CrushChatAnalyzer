# Crush Chat Analyzer 详细使用说明

- **软件名称**：Crush Chat Analyzer
- **版本**：自动迭代（以程序窗口标题为准）
- **制作者**：胡胜杰
- **默认模型**：DeepSeek `deepseek-chat`
- **界面技术**：Python `tkinter/ttk`
- **数据存储**：本地 SQLite

> 这是一个聊天复盘工具，不是读心术，也不能替代真实沟通和边界尊重。

---

## 一、这个工具能做什么

1. 导入微信聊天记录（txt / csv / tsv / json / jsonl）。
2. 用 DeepSeek 分析：
   - 对方可能的情绪；
   - 对方对关系的兴趣和投入信号；
   - 你回复中可能没表达好的地方；
   - 可以怎么调整；
   - 下一步的建议。
3. 不联网也能看本地速览：
   - 消息总数；
   - 我 / 对方消息量；
   - 谁先开启对话；
   - 回复间隔；
   - 平均消息长度；
   - 问号、表情、情绪词数量。
4. 生成智能回复建议：
   - 每条建议都有风格、理由和风险等级；
   - 可以复制；
   - 连接微信后可以直接发送。
5. 微信接入（可选）：
   - 最终 exe 已内置 `wechatauto` 兼容后端；
   - 可读取微信会话；
   - 可导入当前聊天；
   - 可进行自动回复。
6. 显示 API token 和估算费用：
   - 每次调用后显示本次用量；
   - 累计显示总 tokens 和估算费用；
   - 费用单价可自行配置。

---

## 二、安装 Python 和依赖

### 1. 安装 Python

建议 Python 3.10 及以上版本：

```text
https://www.python.org/downloads/
```

安装时注意勾选：

```text
Add Python to PATH
```

验证：

```powershell
python --version
```

### 2. 安装项目依赖

**核心程序不需要安装任何第三方 Python 包。**

- GUI：`tkinter`，Python 自带；
- 数据库：`sqlite3`，Python 自带；
- 网络：`urllib` / `ssl`，Python 自带。

所以正常情况下，只要装了 Python 3.10+，就可以直接运行，不需要安装任何第三方包。

### 3. 如果需要接入微信

最终 exe 已经内置微信自动化后端：

```text
wechatauto 1.2.3
```

所以正常情况下不需要安装 `wxauto`，也不需要 Python 3.11。

如果使用源码版，可以安装兼容后端：

```powershell
python -m pip install wechatauto-replica --no-deps
python -m pip install uiautomation pyperclip Pillow psutil colorama pywin32 cryptography
```

---

## 三、启动程序

### 方式一（推荐，0 基础）

双击项目根目录的：

```text
启动.bat
```

它会自动：

1. 检查 Python 是否安装；
2. 检查 Python 版本是否 3.10+；
3. 如果没安装 Python，自动打开官网下载页；
4. 使用 Python 标准库直接启动程序，不需要 pip 安装依赖。

### 方式二：命令行

```powershell
cd D:\ds_workplace\CrushChatAnalyzer
python main.py
```

或者：

```powershell
python -m crush_analyzer
```

### 方式三：只发一个 exe

执行：

```text
打包成exe.bat
```

会生成单文件：

```text
dist/CrushChatAnalyzer_v<版本号>.exe
```

分发时只需要发送这一个 exe。

它已经内置：

- Python 运行时；
- 图形界面；
- 示例聊天数据；
- 使用说明；
- Markdown 展示和 Token/费用统计。

目标电脑不需要安装 Python，也不需要项目文件夹。

> 直接连接微信仍需目标电脑安装并登录微信 PC 版；自动化后端已经内置在 exe 里。

正常启动后，会出现图形界面。

---

## 四、首次配置 DeepSeek

### 1. 获取 API Key

到 DeepSeek 开放平台注册并创建 API Key：

```text
https://platform.deepseek.com/
```

### 2. 在软件中填写

进入 **设置** 页：

- **API Key**：粘贴 `sk-...`
- **Base URL**：默认 `https://api.deepseek.com`
- **模型**：默认 `deepseek-chat`
- **Temperature**：建议 `0.5 ~ 0.9`
- **Max tokens**：建议 `1000 ~ 2000`
- **超时（秒）**：默认 `90`

点击：

1. **保存设置**
2. **测试 API 连接**

测试成功后，顶部会显示：

```text
API：deepseek-chat ✓
本次 [连接测试] x tokens · ¥0.xxxx
累计：x 次 · x tokens · ¥0.xxxx
```

### 3. 环境变量方式（可选）

也可以不写配置文件，使用环境变量：

```powershell
set DEEPSEEK_API_KEY=sk-xxxx
set DEEPSEEK_BASE_URL=https://api.deepseek.com
set DEEPSEEK_MODEL=deepseek-chat
```

环境变量优先级高于配置文件。

---

## 五、Token 与费用显示说明

### 1. 显示位置

软件顶部信息栏会显示：

- **本次**：最近一次 API 调用的 token 和估算费用；
- **累计**：本机累计调用次数、总 tokens、估算总费用。

每次调用以下功能后都会更新：

- DeepSeek 分析；
- 智能回复建议；
- 微信自动回复；
- API 连接测试。

### 2. 费用怎么算

默认按 1M tokens 的单价估算：

| 项目 | 默认值 |
| --- | --- |
| 缓存命中输入 | 0.5 / 1M |
| 缓存未命中输入 | 2.0 / 1M |
| 输出 | 8.0 / 1M |
| 货币 | CNY |

可在 **设置 → Token 用量与费用估算** 中修改。

计算公式：

```text
估算费用 =
  cache_hit_tokens  / 1,000,000 * 缓存命中输入单价
+ cache_miss_tokens / 1,000,000 * 缓存未命中输入单价
+ completion_tokens / 1,000,000 * 输出单价
```

### 3. 重要提醒

- DeepSeek 的模型价格可能调整；
- 软件显示的是 **估算值**；
- 实际扣费请以 DeepSeek 官方账单为准；
- 点击 **累计用量清零** 可以重新统计本机累计数据。

---

## 六、导入聊天记录

支持格式：

```text
.txt
.csv
.tsv
.json
.jsonl
.ndjson
```

### 1. 使用内置示例数据先试

第一次打开软件时，会自动询问是否导入内置示例。

步骤：

1. 点击 **是**，软件会自动导入内置示例聊天；
2. 在“聊天记录”页点击右上角的 **我是：**，选择 `我`；
3. 点击 **本地速览**，确认数据正常；
4. 配置 API Key 后点击 **DeepSeek 分析**。

### 2. txt 格式

支持多种常见格式。

#### 格式 A：发送人在前

```text
张三 2024-01-01 20:00:00
你好呀

李四 2024-01-01 20:01:00
刚看到，嘿嘿
```

#### 格式 B：时间在前

```text
2024-01-01 20:00:00 张三
你好呀

2024-01-01 20:01:00 李四
刚看到，嘿嘿
```

#### 格式 C：时间和发送人分成两行

```text
2024-01-01 20:00:00
张三
你好呀

2024-01-01 20:01:00
李四
刚看到，嘿嘿
```

#### 格式 D：行内发送人

```text
张三: 你好呀
李四: 刚看到，嘿嘿
```

### 3. CSV / TSV 格式

自动适配常见列名：

| 含义 | 常见列名 |
| --- | --- |
| 发送人 | NickName、Sender、sender、from、Remark、Talker |
| 内容 | StrContent、Content、Msg、message、text |
| 时间 | StrTime、CreateTime、time、timestamp、date |
| 是否我 | IsSender、is_self、fromMe、direction |

示例：

```csv
StrTime,NickName,StrContent,IsSender
2024-01-01 20:00:00,张三,你好,0
2024-01-01 20:01:00,李四,你也好,1
```

### 4. JSON / JSONL 格式

支持 `{"messages":[...]}`、`[...]`、`{"chat":[...]}`、`{"data":[...]}` 等结构。

示例：

```json
{
  "messages": [
    {"sender": "小王", "content": "在干嘛", "time": "2024-01-01 20:00:00", "is_self": true},
    {"sender": "小李", "content": "刚下班，你呢", "time": "2024-01-01 20:05:00", "is_self": false}
  ]
}
```

### 5. 我是谁

导入后请检查 **我是：** 是否正确。

如果选错了：

1. 打开 **聊天记录** 页；
2. 在右上角 **我是：** 下拉框选择你的昵称；
3. 软件会重新标记自己/对方，并保存到本地数据库。

---

## 七、页面使用说明

### 1. 聊天记录

- 左侧是会话列表；
- 右侧是聊天内容；
- 自己靠右，对方靠左；
- 支持搜索关键词；
- 可以切换 **我是：** 来纠正身份；
- 可以删除不需要的会话。

### 2. AI 分析

- **本地速览**：不调用 API；
- **DeepSeek 分析**：调用 DeepSeek；
- **分析重点**：综合解读 / 对方情绪 / 对方态度 / 我的表达 / 下一步行动；
- **补充说明**：可输入例如“我们上周吵过架”；
- **导出报告**：导出为 `.md`；
- **复制**：复制 Markdown 原文。

分析结果支持 Markdown 展示：

- 标题；
- 列表；
- 粗体 / 斜体；
- 行内代码；
- 代码块；
- 引用；
- 链接；
- 简单表格。

### 3. 智能回复

1. 输入想要的效果；
2. 选择生成条数 1~5；
3. 点击 **生成回复建议**；
4. 每条建议显示：
   - 风格；
   - 回复内容；
   - 为什么这么说；
   - 风险等级；
5. 点击 **复制** 或 **发送到微信**。

### 4. 微信接入 / 自动回复

- 安装并连接 `wxauto`；
- 刷新微信会话；
- 选中联系人；
- **读取并导入到左侧**：把当前微信聊天导入分析；
- **设为自动回复对象**：设置自动回复目标；
- **演练模式**：默认开启，只生成不发送；
- **开始 / 停止**：控制后台自动回复。

### 5. 设置

- DeepSeek API Key；
- Base URL / 模型 / Temperature / Max tokens / 超时；
- 分析上下文长度；
- System persona；
- Token 单价和费用显示；
- 微信昵称；
- 自动回复默认参数；
- 清除 API Key；
- 查看配置目录；
- 累计用量清零。

---

## 八、微信接入详细步骤

### 1. 前提

- Windows；
- 微信 PC 版已安装并登录；
- 微信窗口不要最小化；
- 最终 exe 已内置 `wechatauto` 1.2.3 后端。

### 2. 安装

正常情况下不需要额外安装。

如果使用源码版，可以安装兼容后端：

```powershell
python -m pip install wechatauto-replica --no-deps
python -m pip install uiautomation pyperclip Pillow psutil colorama pywin32 cryptography
```

### 3. 连接

> 读取聊天记录时使用本地数据库模式，不会反复弹出/激活微信窗口。只有发送消息时才会调用 GUI。

1. 打开软件；
2. 进入 **微信接入 / 自动回复**；
3. 点击 **连接微信**；
4. 点击 **刷新微信会话**；
5. 在列表中选择联系人。

### 4. 读取聊天

点击 **读取并导入到左侧**。

如果失败：

- 检查微信是否登录；
- 检查窗口是否最小化；
- 点击软件里的 **诊断 wxauto** 查看后端名称和错误；
- 改用 WeChatMsg / PyWxDump 导出文件后导入。

---

## 九、自动回复安全流程

强烈推荐按以下顺序使用：

1. 设置 **我的微信昵称**；
2. 连接微信；
3. 选择联系人；
4. 点击 **设为自动回复对象**；
5. 保持 **演练模式**；
6. 点击 **开始自动回复**；
7. 观察日志里的生成效果；
8. 确认语气、边界和频率都合适；
9. 再取消演练模式，进行真实发送。

软件内置限制：

- 默认只生成不发送；
- 命中“转账、验证码、链接、文件”等关键词时跳过；
- 每分钟发送次数上限；
- 不处理敏感请求。

不建议：

- 对方明确拒绝后继续自动回复；
- 用自动回复查岗、试探、操控；
- 24 小时高频发送；
- 把自动回复伪装成完全自然的人类对话。

---

## 十、Markdown 展示

AI 分析报告支持基础 Markdown：

| 语法 | 效果 |
| --- | --- |
| `# 标题` | 一级标题 |
| `## 标题` | 二级标题 |
| `**加粗**` | 加粗 |
| `*斜体*` | 斜体 |
| `` `代码` `` | 行内代码 |
| ``` 代码块 ``` | 代码块 |
| `- 列表` | 列表 |
| `> 引用` | 引用 |
| `[文字](链接)` | 可点击链接 |

导出报告时保留 Markdown 原文，方便放到 Obsidian、Typora、VS Code 中查看。

---

## 十一、隐私与安全

- 聊天记录保存在本地 SQLite 数据库；
- API Key 保存在本机用户目录配置文件；
- 调用 DeepSeek 时，选中的聊天上下文会发送给模型服务商；
- 请确认你有权处理这些聊天记录；
- 不要随意把导出报告分享给他人；
- 不要公开自己的 API Key；
- AI 分析只是概率推测，不是心理诊断。

---

## 十二、常见问题

### 1. 没有 API Key 能用吗？

可以。导入、聊天查看、本地速览都可以用。
AI 分析和 AI 回复建议需要 API Key。

### 2. 费用显示为 0 怎么办？

可能是：

- 模型接口没有返回 usage；
- 本次调用失败；
- 单价设置为 0；
- 这是本地速览，没有调用 API。

### 3. 费用和 DeepSeek 账单不一致？

软件是估算值，价格会调整，最终请以官方账单为准。

### 4. 导入后全部显示成对方？

在“聊天记录”页右上角把 **我是：** 改成你的昵称。

### 5. wxauto 安装失败？

最终 exe 已经内置兼容后端，正常情况下不需要再安装 `wxauto`。

如果使用源码版，并且原版 wxauto 安装失败，可以改用兼容后端：

```powershell
python -m pip install wechatauto-replica --no-deps
python -m pip install uiautomation pyperclip Pillow psutil colorama pywin32 cryptography
```

### 6. 微信连接失败？

- 确认 Windows 微信 PC 版已登录；
- 确认微信窗口没有最小化；
- 点击软件里的 **诊断 wxauto**，查看后端名称和导入错误；
- 如果当前微信版本不兼容，可以改用文件导入。

### 7. 为什么提示没有安装微信自动化后端？

最终 exe 已经内置，不需要额外安装。  
如果提示未安装，说明你运行的可能是旧版 exe，或者源码环境缺少依赖。

请使用最新版：

```text
dist/CrushChatAnalyzer_v<版本号>.exe
```

软件内点击：

```text
诊断 wxauto
```

可以查看：

- 当前后端名称；
- 版本；
- 当前 Python 解释器；
- 是否打包 exe；
- 导入错误；
- 已搜索的 site-packages 路径。

### 7. 自动回复会回复到自己吗？

如果“我的微信昵称”没填，可能识别错误。
所以默认开启演练模式，强烈建议先填写昵称。

---

## 十三、打包成 exe

方式一：双击项目根目录的：

```text
打包成exe.bat
```

方式二：

```powershell
python -m pip install pyinstaller
python build_exe.py
```

生成文件在：

```text
dist/CrushChatAnalyzer_v<版本号>.exe
```

- 每次打包会自动把 patch 版本号 +1；
- 默认打包为单文件 exe；
- 如果打包机上安装了 wxauto，脚本会自动收集其依赖。

---

## 十四、版本信息

```text
Crush Chat Analyzer
版本：自动迭代（以程序窗口标题为准）
制作者：胡胜杰
默认模型：DeepSeek deepseek-chat
```

祝你沟通顺利，也记得尊重对方的边界。
